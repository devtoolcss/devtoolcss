{
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "format": "prettier --experimental-cli --write ."
  },
  "dependencies": {
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7"
  }
}
