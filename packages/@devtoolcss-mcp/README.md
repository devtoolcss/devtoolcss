## mcp

chrome extension (ws polling) + stateful inspector + lazy init + detach handle (exception & tab toggling dashboard)

### feature

get handle:

- querySelectorAll

handle methods:

- getMatchedStyles
- getComputedStyle (by attr array)
- querySelectorAll
- parent
- children
- attributes
- outerHTML (with depth/line lenght control)
