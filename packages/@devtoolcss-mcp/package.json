{
  "name": "@devtoolcss/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Chrome extension with WebSocket polling to MCP server",
  "type": "module",
  "scripts": {
    "build": "node scripts/copy.js && node scripts/esbuild.config.js",
    "prepare": "pnpm build && pnpm sync:manifest",
    "watch": "node scripts/watch.js",
    "sync:manifest": "node scripts/sync-manifest.js"
  },
  "dependencies": {
    "chrome-inspector": "^1.0.6",
    "js-beautify": "^1.15.4",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.27",
    "chokidar": "^4.0.3",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  }
}
